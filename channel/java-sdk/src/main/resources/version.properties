# 麦芽田渠道开放平台 Java SDK 版本配置
# 对应 Go SDK 中的 version.txt 文件
# 使用语义化版本控制(SemVer)规范

# SDK版本号
# 格式：主版本号.次版本号.修订号[-预发布版本]
sdk.version=2.0.0

# 构建信息（可选）
build.date=${maven.build.timestamp}
build.java.version=${java.version}

# 兼容性信息
min.java.version=8
target.java.version=11